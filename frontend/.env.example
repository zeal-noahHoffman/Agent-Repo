# Base URL of the running agent's HTTP log endpoint, used by the Vite dev proxy.
# The dashboard calls `/api/logs`, which is proxied to ${VITE_API_TARGET}/api/logs.
VITE_API_TARGET=http://localhost:8000
